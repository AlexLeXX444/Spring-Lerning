meta {
  name: POST user
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/user
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
    "firstName": "string",
    "lastName": "string1"
  }
}
